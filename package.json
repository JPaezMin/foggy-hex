{
  "name": "foggy-hex-root",
  "version": "1.0.0",
  "private": true,
  "bin": {
    "deploy-foggy-hex": "./scripts/deploy.js"
  },
  "scripts": {
    "build": "nuxi build",
    "deploy": "node ./scripts/deploy.js"
  },
  "engines": {
    "node": ">=16"
  }
}
