{
  "name": "foggy-hex-root",
  "version": "1.0.0",
  "private": true,
  "bin": {
    "deploy-foggy-hex": "./scripts/deploy.js"
  },
  "scripts": {
    "build": "nuxi build",
    "deploy": "node ./scripts/deploy.js"
  },
  "devDependencies": {
    "nuxi": "^3.14.0"
  },
  "engines": {
    "node": ">=16"
  }
}
